# GitHub About Metadata

GitHub repository About fields are not stored directly in git. Use this page as the copy source for the repository sidebar settings.

## Description

```text
Local MCP context engine for coding agents: bounded repo context, memory, cache proof, handoff packets, and token-saver demos without provider calls.
```

Shorter alternative:

```text
Local-first MCP context engine for coding agents, with bounded repo context, memory, cache proof, handoff packets, and no provider calls.
```

## Website

Leave blank until a real public site is deployed. Do not point the repository at an unfinished, private, or misleading page.

## Topics

```text
mcp
model-context-protocol
coding-agents
ai-agents
context-engine
local-first
developer-tools
rust
token-optimization
repo-indexing
agent-memory
handoff
```

## Release Blurb

```text
Quotarelay 0.1.0 is a local-first MCP context engine for coding agents. It ships bounded repo retrieval, durable local memory, cache visibility, handoff packets, validation recommendations, local demo proof, and token-saver benchmark proof without provider calls, hosted login, telemetry, or billing-savings claims.
```
