# Quotarelay Demo Repo

Tiny local repository used by `scripts/demo-local.ps1`.

The demo copies this fixture to a temporary workspace, syncs it locally, writes a decision memory note, assembles exact and overview context, repeats exact search to show a cache hit, builds a handoff packet, and reports that no provider calls were made.
